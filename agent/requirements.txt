# [VERIFY] funktionierender Stand; PydanticAI/Mem0/LangGraph entwickeln sich
# schnell — bei API-Drift anpassen (siehe README).
fastapi==0.115.6
uvicorn[standard]==0.34.0
httpx==0.28.1
openai==1.59.6

# Variante 1: PydanticAI (OPTIONAL, nicht Default). Die 0.0.20 kennt
#   'pydantic_ai.providers' NICHT -> fuer diese Variante auf 1.x heben:
#   pip install -U 'pydantic-ai-slim[openai]'  (Code nutzt die 1.x-Provider-API).
pydantic-ai-slim[openai]==0.0.20

# Variante 2: LangGraph-Critic
langgraph==0.2.62
langchain-openai==0.2.14

# Gedaechtnis
mem0ai==0.1.40
qdrant-client==1.12.1

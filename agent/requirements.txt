# [VERIFY] funktionierender Stand; PydanticAI/Mem0/LangGraph entwickeln sich
# schnell — bei API-Drift anpassen (siehe README).
fastapi==0.115.6
uvicorn[standard]==0.34.0
httpx==0.28.1
openai==1.59.6

# Variante 1: PydanticAI = Tool-Calling-Agent (jetzt DEFAULT, AGENT_IMPL=pydantic).
#   Braucht 1.x (die alte 0.0.20 kennt 'pydantic_ai.providers' nicht und fiele still
#   auf LangGraph zurueck). Floor-Pin statt exaktem Patch, weil sich die 1.x-API
#   schnell bewegt; agent_pydantic.py faengt den OpenAIModel/OpenAIChatModel-Rename ab.
pydantic-ai-slim[openai]>=1.0,<2

# Variante 2: LangGraph-Critic
langgraph==0.2.62
langchain-openai==0.2.14

# Gedaechtnis
mem0ai==0.1.40
qdrant-client==1.12.1

# Morphik-Auth: selbst signierter HS256-JWT (sonst 401 "Not enough segments")
PyJWT==2.10.1

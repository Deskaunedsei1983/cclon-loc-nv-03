# Loki automatisch als (Default-)Datenquelle in Grafana eintragen -> kein Klicken.
apiVersion: 1

datasources:
  - name: Loki
    uid: loki
    type: loki
    access: proxy
    url: http://loki:3100
    isDefault: true
    editable: true
    jsonData:
      maxLines: 5000

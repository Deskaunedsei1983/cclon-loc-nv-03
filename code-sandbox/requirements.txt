# Stand 2026-08 — versions-auditiert (PyPI latest + CVE-Scan, 22.08.2026).
# Jupyter (fuer OWUI Code-Interpreter) + /run-API.
# jupyterlab 4.6.3 / jupyter-server 2.20.0 schliessen die XSS-/Path-Traversal-Serie
# (u.a. CVE-2026-73415/73417, CVE-2026-44727, CVE-2026-35397).
jupyterlab==4.6.3
jupyter-server==2.20.0
fastapi==0.141.1
uvicorn[standard]==0.52.4

# Office-Dateien (das "Skills"-Aequivalent von claude.ai)
python-docx==1.2.0
openpyxl==3.1.5
XlsxWriter==3.2.9
python-pptx==1.0.2
reportlab==5.0.1
# pypdf 6.16: schliesst DoS-Serie ueber praeparierte PDFs (Sandbox verarbeitet Uploads).
pypdf==6.16.1

# Notebooks programmatisch bauen
nbformat==5.11.1
nbclient==0.11.0

# Daten / Analyse / Plots  (Polars + DuckDB statt pandas-only, wie gewuenscht)
polars==1.43.2
duckdb==1.5.5
# pandas BEWUSST auf dem letzten 2.x-Stand (NICHT 3.x): Die Sandbox fuehrt
# LLM-GENERIERTEN Code aus, und pandas 3 aendert Verhalten TEILS STILL
# (Copy-on-Write: verkettete Zuweisungen mutieren nicht mehr; str- statt
# object-Dtype; 'M'/'Q'/'H'-Frequenzaliase entfernt). Stille Falschergebnisse
# kann der Critic-Loop nicht erkennen — laute Fehler schon. Migration auf 3.x
# spaeter zusammen mit den Codegen-Prompts. Keine offenen CVEs in 2.3.x.
pandas==2.3.3
numpy==2.5.2
matplotlib==3.11.1
# pillow 12.3: PFLICHT-Bump — 33 Advisories seit 11.1 (OOB-Writes/Memory-Corruption
# ueber praeparierte Bilder; die Sandbox verarbeitet fremde Dateien).
pillow==12.3.0
tabulate==0.10.0

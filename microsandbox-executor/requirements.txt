# Stand 2026-08 — versions-auditiert (PyPI latest, 22.08.2026).
fastapi==0.141.1
uvicorn[standard]==0.52.4
# Microsandbox-Python-SDK (microVM-Sandboxen, libkrun). 0.6.x ist ein Rewrite auf
# native Bindings; das Wheel BUENDELT den msb-Runtime + libkrunfw (kein separater
# Server mehr noetig). Gegen die v0.6.13-Stubs verifiziert: Sandbox.create(name,
# image=, cpus=, memory=) und sb.exec(cmd, args) -> ExecOutput mit .exit_code/
# .stdout_text/.stderr_text bestehen fort; stop_and_wait() ist WEG -> run_api.py
# nutzt stop()/remove() mit Fallbacks.
microsandbox==0.6.13
